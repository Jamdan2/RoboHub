ktor {
    deployment {
        port = 9090
        environment = development
        autoreload = true
        watch = [ com.jamdan2.kotlinWebapp ]
    }

    application {
        id = KotlinWebapp
        modules = [ com.jamdan2.kotlinWebapp.MainKt.main ]
    }
}
